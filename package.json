{
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "dev": "nodemon src/server.js",
      "start": "(cd backend/src && npm start)"
    },
}
